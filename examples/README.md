# examples/

Runnable, end-to-end examples: small instrumented applications that emit real
OTel GenAI traffic to a local LLMObs instance, so you can see traces flow from an
app through ingestion to the UI.

Examples double as documentation and as sources of representative (synthetic)
traffic for demos. Point them at a `make dev` lite instance.
