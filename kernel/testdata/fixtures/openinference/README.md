# Fixtures: OpenInference

OpenInference-dialect spans, normalized in the kernel hot path onto the canonical
model. Each fixture pairs the raw OpenInference payload with the expected
canonical output; raw attributes must be preserved alongside canonical fields.

Synthetic data only. See the parent [README](../README.md) for the recording
procedure.
