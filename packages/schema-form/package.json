{
  "name": "@llmobs/schema-form",
  "version": "0.1.0",
  "private": true,
  "description": "Render a plugin settings JSON Schema (the supported subset) as a form using @llmobs/ui. Client-side validation mirrors the kernel validator; writeOnly fields are write-only secrets that are never rendered back (J2, ADR-0024).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "react": "^18.3.0"
  },
  "dependencies": {
    "@llmobs/ui": "workspace:*",
    "@llmobs/tokens": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "18.3.12",
    "react": "18.3.1",
    "typescript": "5.6.3"
  }
}
