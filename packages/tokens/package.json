{
  "name": "@llmobs/tokens",
  "version": "0.1.0",
  "private": true,
  "description": "LLMObs design tokens: CSS custom properties (light/dark) plus typed accessors. The single source of color, spacing, radii, and type scale — plugins consume variables only, never hex.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" },
    "./tokens.css": "./src/tokens.css"
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": { "typescript": "5.6.3" }
}
